# packages.conary.io (Package Index)

This is the package browsing/search frontend for the Remi server, served at **packages.conary.io**.

**Deploy target:** `/conary/web` on the Remi server

```bash
# Build and deploy
../deploy/deploy-sites.sh packages
```

This is NOT the main site. For conary.io, see `../site/`.
