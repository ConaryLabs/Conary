# conary.io (Main Site)

This is the marketing/landing site for Conary, served at **conary.io**.

**Deploy target:** `/conary/site` on the Remi server

```bash
# Build and deploy
../deploy/deploy-sites.sh site
```

This is NOT the packages site. For packages.conary.io, see `../web/`.
