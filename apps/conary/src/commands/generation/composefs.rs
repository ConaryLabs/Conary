// src/commands/generation/composefs.rs
//! Re-exports from conary-core. Composefs detection lives in the core crate
//! so the transaction engine can access it directly.
pub use conary_core::generation::composefs::*;
