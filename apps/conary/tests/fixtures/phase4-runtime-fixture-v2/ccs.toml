[package]
name = "phase4-runtime-fixture"
version = "1.0.1"
version_scheme = "conary"
release = "1"
kind = "package"
description = "Phase 4 runtime fixture v2 for exact native update and rollback coverage"
license = "MIT"

[package.platform]
os = "linux"
arch = "x86_64"
libc = "gnu"

[provides]
capabilities = ["phase4-runtime-fixture"]
binaries = ["/usr/bin/phase4-runtime-fixture"]

[components]
default = ["runtime"]

[[config.files]]
source = "ccs"
path = "/etc/phase4-runtime-fixture/app.conf"
noreplace = true
payload = "matched"

[hooks]
