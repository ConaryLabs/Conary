[package]
name = "dep-circular-a"
version = "1.0.0"
description = "Circular dependency fixture A"
license = "MIT"

[package.platform]
os = "linux"
arch = "x86_64"
libc = "gnu"

[provides]
capabilities = ["dep-circular-a"]
binaries = ["/usr/bin/dep-circular-a-v1"]

[requires]
capabilities = []
packages = [
    { name = "dep-circular-b", version = ">=1.0" },
]

[components]
default = ["runtime"]

[hooks]
