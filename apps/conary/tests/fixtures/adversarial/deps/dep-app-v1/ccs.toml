requirements = [
    { kind = "Depends", behavior = "Hard", expression = { operator = "atom", operands = { name = "dep-liba", capability_kind = "PackageName", version_constraint = "=1.0.0", native_text = "dep-liba =1.0.0" } }, alternatives = [{ name = "dep-liba", capability_kind = "PackageName", version_constraint = "=1.0.0", native_text = "dep-liba =1.0.0" }], native_text = "dep-liba =1.0.0" },
    { kind = "Depends", behavior = "Hard", expression = { operator = "atom", operands = { name = "dep-libb", capability_kind = "PackageName", version_constraint = ">=1.0.0", native_text = "dep-libb >=1.0.0" } }, alternatives = [{ name = "dep-libb", capability_kind = "PackageName", version_constraint = ">=1.0.0", native_text = "dep-libb >=1.0.0" }], native_text = "dep-libb >=1.0.0" },
]

[package]
name = "dep-app"
version = "1.0.0"
version_scheme = "conary"
release = "1"
kind = "package"
description = "Application with chained dependency requirements"
license = "MIT"

[package.platform]
os = "linux"
arch = "x86_64"
libc = "gnu"

[provides]
capabilities = ["dep-app"]
binaries = ["/usr/bin/dep-app-v1"]

[components]
default = ["runtime"]

[hooks]
