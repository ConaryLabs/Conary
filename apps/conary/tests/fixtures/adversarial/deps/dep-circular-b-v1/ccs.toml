requirements = [
    { kind = "Depends", behavior = "Hard", expression = { operator = "atom", operands = { name = "dep-circular-a", capability_kind = "PackageName", version_constraint = ">=1.0.0", native_text = "dep-circular-a >=1.0.0" } }, alternatives = [{ name = "dep-circular-a", capability_kind = "PackageName", version_constraint = ">=1.0.0", native_text = "dep-circular-a >=1.0.0" }], native_text = "dep-circular-a >=1.0.0" },
]

[package]
name = "dep-circular-b"
version = "1.0.0"
version_scheme = "conary"
release = "1"
kind = "package"
description = "Circular dependency fixture B"
license = "MIT"

[package.platform]
os = "linux"
arch = "x86_64"
libc = "gnu"

[provides]
capabilities = ["dep-circular-b"]
binaries = ["/usr/bin/dep-circular-b-v1"]

[components]
default = ["runtime"]

[hooks]
