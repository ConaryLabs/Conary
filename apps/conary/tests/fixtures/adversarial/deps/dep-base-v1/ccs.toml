[package]
name = "dep-base"
version = "1.0.0"
version_scheme = "conary"
release = "1"
kind = "package"
description = "Base dependency fixture version 1"
license = "MIT"

[package.platform]
os = "linux"
arch = "x86_64"
libc = "gnu"

[provides]
capabilities = ["dep-base", "dep-base-api-v1"]
binaries = ["/usr/bin/dep-base-v1"]

[components]
default = ["runtime"]

[hooks]
