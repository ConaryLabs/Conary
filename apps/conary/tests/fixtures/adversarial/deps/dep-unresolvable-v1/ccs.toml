requirements = [
    { kind = "Depends", behavior = "Hard", expression = { operator = "atom", operands = { name = "nonexistent-package", capability_kind = "PackageName", version_constraint = ">=1.0.0", native_text = "nonexistent-package >=1.0.0" } }, alternatives = [{ name = "nonexistent-package", capability_kind = "PackageName", version_constraint = ">=1.0.0", native_text = "nonexistent-package >=1.0.0" }], native_text = "nonexistent-package >=1.0.0" },
]

[package]
name = "dep-unresolvable"
version = "1.0.0"
version_scheme = "conary"
release = "1"
kind = "package"
description = "Fixture with an unresolvable dependency"
license = "MIT"

[package.platform]
os = "linux"
arch = "x86_64"
libc = "gnu"

[provides]
capabilities = ["dep-unresolvable"]
binaries = ["/usr/bin/dep-unresolvable-v1"]

[components]
default = ["runtime"]

[hooks]
