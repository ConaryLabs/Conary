[package]
name = "adversarial-truncated"
version = "1.0.0"
description = "Base fixture for truncated object tests"
license = "MIT"

[package.platform]
os = "linux"
arch = "x86_64"
libc = "gnu"

[provides]
capabilities = ["adversarial-truncated"]
binaries = ["/usr/bin/hello"]

[requires]
capabilities = []
packages = []

[components]
default = ["runtime"]

[hooks]
